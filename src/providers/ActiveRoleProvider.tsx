import React from 'react';

interface ActiveRoleProviderProps {
  children: React.ReactNode;
}

export function ActiveRoleProvider({ children }: ActiveRoleProviderProps) {
  // Placeholder provider - can be enhanced later
  return <>{children}</>;
}